version: '2.1'

services:
    mssql:
      extends:
        file: ./mssql/docker-compose.yml
        service: db
    mssql2017:
      extends:
        file: ./mssql/docker-compose.yml
        service: db2017
    mysql:
      extends:
        file: ./mysql/docker-compose.yml
        service: db
    mysqlv5:
      extends:
        file: ./mysql/docker-compose.yml
        service: dbv5
    pgsql:
      extends:
        file: ./pgsql/docker-compose.yml
        service: db
