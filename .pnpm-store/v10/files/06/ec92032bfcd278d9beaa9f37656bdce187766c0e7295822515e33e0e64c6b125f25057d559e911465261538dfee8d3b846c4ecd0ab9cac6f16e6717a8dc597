export interface Options {
    field: string;
    size?: [number, number];
    round?: boolean;
    ratio?: number;
    padding?: number;
    sort?: (a: any, b: any) => any;
    as?: [string, string];
    ignoreParentValue?: boolean;
}
export declare function partition(data: any, options: Options): any[];
