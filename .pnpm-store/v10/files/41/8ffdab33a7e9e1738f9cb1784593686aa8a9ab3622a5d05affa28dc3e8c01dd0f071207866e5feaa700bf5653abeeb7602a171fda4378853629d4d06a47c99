import { ShapeInfo } from '../../interface';
/**
 * @ignore
 * Determines whether model is change
 * @param currentModel
 * @param preModel
 * @returns
 */
export declare function isModelChange(currentModel: ShapeInfo, preModel: ShapeInfo): boolean;
