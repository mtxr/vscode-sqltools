import { IG } from '../dependents';
/**
 * 通过名字获取渲染 engine
 * @param name 渲染引擎名字
 * @returns G engine
 */
export declare function getEngine(name: string): IG;
/**
 * 注册渲染引擎
 * @param name
 * @param engine
 */
export declare function registerEngine(name: string, engine: IG): void;
