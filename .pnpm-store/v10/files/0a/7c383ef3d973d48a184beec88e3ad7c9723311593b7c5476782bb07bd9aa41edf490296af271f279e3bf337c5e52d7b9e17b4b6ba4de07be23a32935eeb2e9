import { BulletOptions } from './types';
type TransformData = {
    min: number;
    max: number;
    ds: any[];
};
/**
 * bullet 处理数据
 * @param options
 */
export declare function transformData(options: BulletOptions): TransformData;
export {};
